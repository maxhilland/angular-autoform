'use strict';

describe('autoform.form module', function() {

  beforeEach(module('autoform.form'));

  describe('form controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainCtrl = $controller('MainCtrl');
      expect(mainCtrl).toBeDefined();
    }));

  });
});
